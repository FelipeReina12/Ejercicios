'''Realice un progrma que indique el tipo de triángulo según el valor de los lados que ingrese el ususario'''

def triangle_type(a, b, c):
    if a == b and b == c:
        return "Equilátero"
    elif a == b or b == c or a == c:
        return "Isósceles"
    else:
        return "Escaleno"

a = int(input("Ingresa el primer lado: "))
b = int(input("Ingresa el segundo lado: "))
c = int(input("Ingresa el tercer lado: "))

result = triangle_type(a, b, c)

print("El triángulo es:", result)
